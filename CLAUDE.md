# TestGuard

Follow `AGENTS.md` — it is the project's workflow and rules for every agent.

If a `CLAUDE.local.md` exists it carries the maintainer's local tooling
(OpenWolf context hooks) and is not part of the repository.
